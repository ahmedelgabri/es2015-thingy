# How to run the app

## To run the app

```sh
$ npm i && npm start
```

This will run the `webpack-dev-server` and you will be able to view the app on `localhost:8080`

## To build the app

```sh
$ npm run build
```
